[package]
name = "daml-ide-bridge"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"
description = "Shows Daml script results in the editor by proxying the Daml language server"

[dependencies]
serde_json = "1"
